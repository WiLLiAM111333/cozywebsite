import { CozyClient } from "../../../lib";
import { MessageReaction } from 'discord.js';

export const event = (client: CozyClient, reaction: MessageReaction): void => {
  // client.starBoardManager.handleReactionRemove(reaction);
}
