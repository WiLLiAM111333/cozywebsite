// These are only to be used as query parameters for the Quotable API 

export enum SortBy {
  NAME = 'name',
  QUOTE_COUNT = 'quoteCount'
}

export enum SortOrder {
  ASCENDING = 'asc',
  DESCENDING = 'desc'
}
